% layout 'helptableview';
% title 'Help table';
<P ALIGN=center><h2>Таблица ингредиентов</h2></P>
<table border=1>
<tr><td>&nbsp;</td>
%	foreach my $key(keys %{$ingredients}){#Печатаем список ингредиентов
<td align=center><%= $ingredients->{$key}{'INGREDIENT'}%><br/><%= $ingredients->{$key}{'TOTAL'}%> <%= $ingredients->{$key}{'MEASURE'}%></td>
%	}
</tr>
% my $currentrecipeid=0;
% my @printingredients=();
% foreach my $ascsort(@{$ascsort}) {
% 	#foreach my $recipeid(keys %{$recipes}){#Печатаем список блюд
% 		if($recipes->{$ascsort->[0]}{'RECIPEID'}!=$currentrecipeid){$currentrecipeid=$recipes->{$ascsort->[0]}{'RECIPEID'};
%		    if(@printingredients){
%			foreach my $ingredientid(keys %{$ingredients}){			
			    <td align=center>
%				if(!$printingredients[$ingredientid]){
				    -
%				}else{
				    <%= $printingredients[$ingredientid] %>
%				}
			    </td>
%			}
%			@printingredients=(); 
			</tr>
%		     }
		    <tr><td><%= $recipes->{$ascsort->[0]}{'RECIPE'} %>, <%= $recipes->{$ascsort->[0]}{'YIELD'} %></td>
%		}else{
%		}
%		foreach my $ingredientid(keys %{$ingredients}){
%			if ($ingredientid==$recipes->{$ascsort->[0]}{'INGREDIENTID'}){
%				$printingredients[$ingredientid]=$recipes->{$ascsort->[0]}{'VALUE'};
%			}
%		}
%	#}
%}
%if(@printingredients){
%			foreach my $ingredientid(keys %{$ingredients}){			
			    <td align=center>
%			    if(!$printingredients[$ingredientid]){
				    -
%				}else{
				    <%= $printingredients[$ingredientid] %>
%				}
			    </td>
%			}
			</tr>
%		     }
</table>